import 'package:flutter/material.dart';

class AlbumTitleText extends StatelessWidget {
  const AlbumTitleText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Albums',
      style: TextStyle(color: Colors.black),
    );
  }
}
